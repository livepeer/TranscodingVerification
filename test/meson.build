metrics_test = executable('metrics', 'metrics.c',
                      include_directories : inc,
                      dependencies : deps,
                      link_with : libmea)
test('test metrics', metrics_test)
